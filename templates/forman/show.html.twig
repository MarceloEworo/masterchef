{% extends 'base.html.twig' %}

{% block title %}Forman{% endblock %}

{% block body %}
    <h1>Forman</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Rol</th>
                <td>{{ forman.rol }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('forman_index') }}">back to list</a>

    <a href="{{ path('forman_edit', {'nombreEquipo': forman.nombreEquipo}) }}">edit</a>

    {{ include('forman/_delete_form.html.twig') }}
{% endblock %}

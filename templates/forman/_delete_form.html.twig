<form method="post" action="{{ path('forman_delete', {'nombreEquipo': forman.nombreEquipo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forman.nombreEquipo) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Equipos{% endblock %}

{% block body %}
    <h1>Equipos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>NombreEquipo</th>
                <td>{{ equipo.nombreEquipo }}</td>
            </tr>
            <tr>
                <th>Triptico</th>
                <td>{{ equipo.triptico }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('equipos_index') }}">back to list</a>

    <a href="{{ path('equipos_edit', {'nombreEquipo': equipo.nombreEquipo}) }}">edit</a>

    {{ include('equipos/_delete_form.html.twig') }}
{% endblock %}

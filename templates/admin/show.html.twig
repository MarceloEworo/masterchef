{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdAdmin</th>
                <td>{{ admin.idAdmin }}</td>
            </tr>
            <tr>
                <th>Usurio</th>
                <td>{{ admin.usurio }}</td>
            </tr>
            <tr>
                <th>Clave</th>
                <td>{{ admin.clave }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_index') }}">back to list</a>

    <a href="{{ path('admin_edit', {'idAdmin': admin.idAdmin}) }}">edit</a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Admin index{% endblock %}

{% block body %}
    <h1>Admin index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdAdmin</th>
                <th>Usurio</th>
                <th>Clave</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for admin in admins %}
            <tr>
                <td>{{ admin.idAdmin }}</td>
                <td>{{ admin.usurio }}</td>
                <td>{{ admin.clave }}</td>
                <td>
                    <a href="{{ path('admin_show', {'idAdmin': admin.idAdmin}) }}">show</a>
                    <a href="{{ path('admin_edit', {'idAdmin': admin.idAdmin}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_new') }}">Create new</a>
{% endblock %}

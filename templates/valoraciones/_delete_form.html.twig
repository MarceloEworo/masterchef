<form method="post" action="{{ path('valoraciones_delete', {'idValoraciones': valoracione.idValoraciones}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ valoracione.idValoraciones) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('jueces_delete', {'idJuez': juece.idJuez}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ juece.idJuez) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('concursantes_delete', {'idConcursante': concursante.idConcursante}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ concursante.idConcursante) }}">
    <button class="btn">Delete</button>
</form>
